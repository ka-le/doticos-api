deployment:
  staging:
    branch: master
    heroku:
      appname: doticos-v2-api
dependencies:
  pre:
    - go get github.com/tools/godep
  post:
    - go get -u github.com/tools/godep
    - godep save -v

test:
  pre:
    - go vet ./...

  override:
    - godep go test ./...